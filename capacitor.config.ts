import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'avion-front-end',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
